# ssss activity workspace

This repository contains helper scripts to automate GitHub activity.

- `backfill_commits.py` writes dated entries to `keep.log`.
- `auto_activity.py` bootstraps PR + Issue automation.
- `activity_automation.py` orchestrates large batches of commits, PRs, and issues.

Run `python3 activity_automation.py --help` for usage examples.
